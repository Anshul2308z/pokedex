export default function cleanInput(str : string):string[]{
    let toReturn: string[] = str.toLowerCase().trim().split(/\s+/);
    return toReturn 
}

   // split on 1+ whitespace characters
   
   interface ReadInput {
       input : <Stream>
       output : <stream>
   }
function startREPL(){

}


